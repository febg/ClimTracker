//
//  Gesture.swift
//  ClimbTracker
//
//  Created by Felipe Ballesteros on 2017-10-08.
//  Copyright © 2017 Felipe Ballesteros. All rights reserved.
//

import Foundation
/*
 let panGestureRecognizer = UIPanGestureRecognizer(target: self, action: Selector(("handleTap:")))
 preludeView.addGestureRecognizer(panGestureRecognizer)
 
 
 
 @objc func panBlurButton(sender: UIPanGestureRecognizer) {
 
 print( sender.location(in: self.view))
 var xPosition = sender.location(in: self.view).x
 var yPosition = sender.location(in:self.view).y
 print(type(of: xPosition),Hexagon.frame.maxX)
 if ( Float(xPosition) <= Float(lowerRightH.frame.maxX) && Float(yPosition) >= Float(lowerRightH.frame.minY) && Float(xPosition) >= Float(lowerRightH.frame.minX) && Float(yPosition) <= Float(lowerRightH.frame.maxY)){
 self.upperH.alpha = 1.0
 self.lowerH.isHighlighted = false
 self.upperRightH.isHighlighted = true
 self.lowerH.isHighlighted = false
 self.lowerRightH.isHighlighted = true
 self.lowerLeftH.isHighlighted = false
 self.upperLeft.isHighlighted = false
 level = 2
 }
 
 
 if (Float(xPosition) >= Float(upperRightH.frame.minX) && Float(xPosition) <= Float(upperRightH.frame.maxX) && Float(yPosition) >= Float(upperRightH.frame.minY) && Float(yPosition) <= Float(upperRightH.frame.maxY)){
 self.upperH.isHighlighted = true
 self.lowerH.isHighlighted = false
 self.upperRightH.isHighlighted = true
 self.lowerH.isHighlighted = false
 self.lowerRightH.isHighlighted = false
 self.lowerLeftH.isHighlighted = false
 self.upperLeft.isHighlighted = false
 level = 1
 }
 
 if (Float(xPosition) >= Float(upperH.frame.minX) && Float(xPosition) <= Float(upperH.frame.maxX) && Float(yPosition) >= Float(upperH.frame.minY) && Float(yPosition) <= Float(upperH.frame.maxY)){
 self.upperH.alpha = 1
 print("alpha")
 self.lowerH.isHighlighted = false
 self.upperRightH.isHighlighted = false
 self.lowerH.isHighlighted = false
 self.lowerRightH.isHighlighted = false
 self.lowerLeftH.isHighlighted = false
 self.upperLeft.isHighlighted = false
 level = 0
 }
 if (Float(xPosition) <= lower[1] && Float(xPosition) >= lower[0] && Float(yPosition) >= lower[2] && Float(yPosition) <= lower[3]){
 self.upperH.isHighlighted = true
 self.lowerH.isHighlighted = true
 self.upperRightH.isHighlighted = true
 self.lowerH.isHighlighted = true
 self.lowerRightH.isHighlighted = true
 self.lowerLeftH.isHighlighted = false
 self.upperLeft.isHighlighted = false
 level = 3
 
 }
 if (Float(xPosition) <= lowerLeftt[1] && Float(xPosition) >= lowerLeftt[0] && Float(yPosition) >= lowerLeftt[2] && Float(yPosition) <= lowerLeftt[3]){
 self.upperH.isHighlighted = true
 self.lowerH.isHighlighted = true
 self.upperRightH.isHighlighted = true
 self.lowerH.isHighlighted = true
 self.lowerRightH.isHighlighted = true
 self.lowerLeftH.isHighlighted = true
 self.upperLeft.isHighlighted = false
 level = 4
 }
 if (Float(yPosition) <= upperLeftt[3] && Float(yPosition) >= upperLeftt[2] && Float(xPosition) >= upperLeftt[0] && Float(xPosition) <= upperLeftt[1]){
 self.upperH.isHighlighted = true
 self.lowerH.isHighlighted = true
 self.upperRightH.isHighlighted = true
 self.lowerH.isHighlighted = true
 self.lowerRightH.isHighlighted = true
 self.lowerLeftH.isHighlighted = true
 self.upperLeft.isHighlighted = true
 level = 5
 }
 
 }*/
